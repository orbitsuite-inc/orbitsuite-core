# OrbitSuite Core (.env.example)
# Only variables documented in README. Do not add demo cap or local LLM knobs in Core.

# OpenAI key (paste after 2-call demo cap hit, or upfront to skip demo)
OPENAI_API_KEY=

# Demo relay (optional maintainer-provided two-call path)
DEMO_MODE_ENABLED=true
DEMO_RELAY_URL=http://127.0.0.1:5057/v1/chat/completions
# Optional shared secret header value (sent as X-Orbit-Demo)
DEMO_RELAY_AUTH=
